# GKE
Google Kubernetes Engine (GKE) PlayGround

## Prerequisites

1. Install Cloud SDK
2. Install kubectl
3. Login to GCP

```bash
# Install Google SDK
brew install google-cloud-sdk

# Install kubectl
brew install kubernetes-cli

# Login to GCP
gcloud init

# Add your account to the ADC
gcloud auth application-default login

```